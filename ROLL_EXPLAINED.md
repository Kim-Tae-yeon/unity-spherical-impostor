# 회전(Roll) 완전 설명서

> 이 문서는 임포스터의 **회전 보정(roll)** 을, 처음 보는 사람도 읽기만 하면
> **직접 다시 구현할 수 있을 만큼** 하나하나 풀어 설명합니다.
> 필요한 배경지식: 각도, sin/cos, 벡터 내적/외적, 점 회전. (딱 고등학교 + α)

---

## 목차
1. 임포스터가 뭔지 (30초)
2. 왜 회전이 필요한가
3. 지구본으로 감 잡기
4. 준비물: 빌보드의 좌표축 세 개
5. "지금 어느 사진을 봐야 하나" — yaw/pitch 고르기
6. 핵심: 돌릴 각도 `yRot` 만들기
7. `yRot = sin·δφ` 를 손으로 유도
8. 그 각도로 그림을 실제로 돌리기
9. 숫자로 따라가 보기
10. 함정들 (부호, seam, 극)
11. 이제 직접 구현 (요약 레시피)

---

## 1. 임포스터가 뭔지 (30초)

멀리 있는 나무를 진짜 3D로 그리면 비쌉니다. 그래서 나무를 **여러 각도에서 미리 찍어**
사진 격자(아틀라스)로 저장해두고, 카메라가 보는 방향에 맞는 사진 한 장을 골라
**항상 카메라를 향하는 판때기(빌보드)** 에 붙입니다.

- 가로 = 빙 도는 방향(**yaw**), 세로 = 위/아래에서 보는 정도(**pitch**).
- 예: 8×8 = 64장.

이 문서의 주제는 그중 **"사진을 판때기에 붙일 때 살짝 돌려주는" 부분**입니다.

---

## 2. 왜 회전이 필요한가

사진은 예를 들어 **45° 간격**으로 띄엄띄엄 찍었는데, 카메라는 그 사이(예: 30°)로
**부드럽게** 움직입니다. 가장 가까운 사진을 그냥 붙이면, 사진이 찍힌 방향과
지금 보는 방향이 조금 달라서 그림이 **미세하게 기울어져(roll)** 보입니다.

특히 위/아래에서 볼 때 이 기울어짐이 심해집니다. 그대로 두면 카메라를 돌릴 때마다
스프라이트가 **덜컥덜컥 틀어지며** 이음매가 보입니다.

👉 목표: 이 기울어질 양을 **미리 계산해서, 판때기를 반대로 그만큼 돌려** 항상 똑바로 세우기.

---

## 3. 지구본으로 감 잡기 🌏

지구본의 세로줄(경도선)을 떠올리세요.

- **적도**에서는 경도선이 거의 **수직**입니다 → 옆으로 좀 움직여도 안 기울어요.
- **극(북/남극)** 에서는 경도선이 한 점으로 **모입니다** → 조금만 움직여도 확 돌아갑니다.

여기서 두 가지 직감을 얻습니다. **회전량은**
1. **위/아래로 볼수록(극에 가까울수록) 커진다.**
2. **가장 가까운 사진에서 좌우로 벗어난 만큼 커진다.**

공식은 정확히 이 둘의 **곱**입니다. 뒤에서 그대로 나옵니다.

---

## 4. 준비물: 빌보드의 좌표축 세 개

회전은 "판때기 평면 안에서" 일어납니다. 그 평면을 정의할 축이 먼저 필요합니다.

`dir` = **오브젝트(나무) 중심에서 카메라로 향하는 단위 벡터**. (지금 보는 방향의 반대)

이걸로 판때기의 축 세 개를 만듭니다:

```
up    = (0, 1, 0)                     // 월드의 위쪽 (고정)
hori  = normalize(cross(dir, up))     // 판때기의 "오른쪽"
vertV = cross(hori, dir)              // 판때기의 "위쪽"
```

- `cross(dir, up)`: `dir`과 `up` 둘 다에 수직인 벡터 → 화면 기준 **가로축**.
- `vertV = cross(hori, dir)`: 나머지를 채워 **세로축**. 이건 사실 **월드의 위쪽을 화면 평면에 눕힌 것**입니다.
- `{hori, vertV, dir}` 은 서로 직각인 정규직교 기저. 판때기는 `hori–vertV` 평면 위에 있고 `dir`을 바라봅니다.

> **꼭 기억:** `vertV`는 "월드 위쪽"을 기준으로 만들어집니다. 사진을 찍을(베이크할) 때도
> 카메라의 up을 **월드 위쪽으로** 두어야 합니다. 이 둘이 같은 기준이라서 회전 보정이 성립합니다.

---

## 5. "지금 어느 사진을 봐야 하나" — yaw/pitch 고르기

돌릴 각도를 만들기 전에, **어느 열/행 사진을 고를지**부터 정합니다.
회전 공식이 이 값들을 재활용하기 때문에 같이 봅니다.

### 5-1. yaw (가로, 열 고르기)

```
verticalAngle = frac( atan2(-dir.z, -dir.x) / (2π) ) * FramesX + 0.5
열(col) = floor(verticalAngle)
```

한 조각씩:

- `atan2(-dir.z, -dir.x)` : `-dir`이 가리키는 **좌우 방위각**(라디안, -π~π). 나무 주위 몇 시 방향인지.
- `/(2π)` : 한 바퀴를 **0~1로 정규화**.
- `frac(...)` : 소수부만 취해 **0~1로 감쌈**. 덕분에 **360°와 0°가 자동으로 이어집니다**(seam 처리).
- `* FramesX` : 0~FramesX 범위로. 정수부가 곧 열 번호.
- `+ 0.5` : 반 칸 밀기. 이게 있어서 **`frac(verticalAngle) = 0.5`일 때가 그 열의 사진을 찍은 정확한 각도**가 됩니다. (아주 중요 — 6절에서 다시 씀)

`floor`로 정수부를 취하면 지금 봐야 할 **열**이 나옵니다.

### 5-2. pitch (세로, 행 고르기)

```
verticalDot = dot(dir, up)             // = dir.y = sin(고도각)
upAngle = acos(-verticalDot) / π + 0.5/(FramesY-1)
행(row) = min( floor(upAngle * (FramesY-1)), FramesY-1 )
```

- `verticalDot = dir.y` : 카메라가 얼마나 위/아래에 있는지. 정면=0, 꼭대기=+1, 바닥=-1.
  수학적으로 **sin(고도각)** 입니다. (6절 회전량의 첫 번째 재료)
- `acos(-dir.y)` : 바닥(dir.y=-1)→0, 꼭대기(dir.y=+1)→π 인 각. `/π` 로 0~1.
- `+ 0.5/(FramesY-1)` : yaw의 `+0.5`와 같은 역할(반 칸 밀기). floor가 행 중앙에 맞게.
- `FramesY-1` : 맨 위·맨 아래 행이 각각 극을 담당해 **간격이 FramesY-1개**라서.

---

## 6. 핵심: 돌릴 각도 `yRot` 만들기

이제 회전량입니다. 실제 코드 한 줄:

```
yRot = (1/FramesX) * π * verticalDot * (2*frac(verticalAngle) - 1)
```

이걸 **의미 있는 두 덩어리**로 묶어 읽습니다.

### 덩어리 ① `verticalDot` = sin(고도각) — "얼마나 위/아래에서 보나"

- 정면(적도): dir.y = 0 → **0** → 안 돌림.
- 꼭대기/바닥(극): dir.y = ±1 → **±1** → 최대로 돌림.

3절의 직감 1 그대로입니다.

### 덩어리 ② `(2*frac(verticalAngle) - 1) * (π/FramesX)` = δφ — "사진에서 벗어난 각"

- `frac(verticalAngle)` : 지금 고른 열 **안에서의 위치**(0~1).
- `+0.5` 덕분에 **0.5 = 사진 찍은 정확한 각도**(중심), 0 또는 1 = 양쪽 경계.
- `2*frac - 1` : 이걸 **-1~+1**로 바꿈. 중심에서 0, 왼쪽 -1, 오른쪽 +1.
- `× (π/FramesX)` : 한 열 각도폭(2π/FramesX)의 절반을 곱함 → **실제 벗어난 방위각 라디안**.
  범위는 `±π/FramesX` (= ±반 칸).

이 둘의 곱:

```
yRot = sin(고도각) × δφ
     = (극에 가까운 정도) × (가장 가까운 사진에서 벗어난 좌우 각)
```

3절의 두 직감이 정확히 곱으로 합쳐졌습니다.

---

## 7. `yRot = sin·δφ` 를 손으로 유도

왜 하필 **곱하기**이고 왜 **sin**일까요? 구면 위 성질에서 나옵니다.

카메라가 항상 중심을 바라보고, up을 월드 위쪽으로 유지한 채 **방위각만 아주 조금 `dφ`** 돌린다고 합시다.
이때 화면(시야)이 시선축을 기준으로 얼마나 도는지를 봅니다.

- 극에 있을 때(고도 90°): 방위각을 `dφ` 돌리면 화면이 **그대로 `dφ`** 만큼 돕니다.
  (북극 위에서 빙 도는 걸 상상 — 방위각 = 화면 회전)
- 적도에 있을 때(고도 0°): 방위각을 돌려도 화면은 **옆으로 슬라이드만**, 회전 0.
- 그 사이 고도 θ: 회전량은 정확히 **`sin(θ) · dφ`**.
  (고도가 올라갈수록 경도선이 모이며 회전 비중이 sin으로 늘어남)

작은 조각들을 다 더하면(적분):

```
총 회전 = ∫ sin(θ) dφ = sin(θ) · δφ
```

그래서 `yRot = sin(고도각) · δφ`. 지구본에서 극으로 갈수록 경도선이 모이는 그 현상의 수식판입니다.

> 참고: `sin(θ)`가 코드에선 `verticalDot = dir.y`로 바로 나옵니다.
> `dir`이 단위벡터라 `dir.y = sin(고도각)`이 성립하기 때문입니다.

---

## 8. 그 각도로 그림을 실제로 돌리기

`yRot`이 정해졌으면 판때기의 네 모서리 점을 그 각도만큼 돌립니다. 고교 **점 회전 공식**:

```
cy = cos(yRot);  sy = sin(yRot);

새 x =  x*cy + y*sy
새 y = -x*sy + y*cy
```

(코드에선 `mul(uvExpansion, float2x2(cy,-sy, sy,cy))` 한 줄. 위 식과 완전히 같습니다.)

그리고 돌린 좌표로 판때기 위치를 만듭니다:

```
billboard = hori * (새 x) + vertV * (새 y)
```

### ★ 미묘하지만 중요한 점: "위치는 돌리고, UV는 안 돌린다"

- **판때기 정점 위치**는 위처럼 `yRot` 돌린 좌표로 만듭니다.
- 하지만 **텍스처 UV(어느 픽셀을 읽을지)** 는 **돌리기 전 원래 좌표**로 계산합니다.

결과적으로 텍스처는 원래 모서리에 붙어 있는데 **모서리(정점)만 돌아가므로,
화면에 그려지는 그림 전체가 `yRot`만큼 굴러갑니다.** 이 굴림이 2절의 기울어짐을 정확히 상쇄합니다.

> 만약 UV까지 같이 돌리면 아무 일도 안 일어납니다(그림과 판이 같이 돌아 상쇄됨). 그래서
> **위치만** 돌리는 게 핵심입니다.

---

## 9. 숫자로 따라가 보기 (8×8 기준)

가로 8장 → 사진은 **45° 간격**. 한 칸의 절반 = 22.5° = `π/8` 라디안.

| 상황 | sin(고도각) | 벗어난 각 δφ | yRot = 곱 |
|---|---|---|---|
| 정면(0°), 두 사진 정중간 | sin 0° = 0 | 22.5° | **0°** (적도, 안 기움) |
| 비스듬(30°), 정중간 | sin 30° = 0.5 | 22.5° | **≈ 11.25°** |
| 거의 꼭대기(90°), 정중간 | sin 90° = 1 | 22.5° | **≈ 22.5°** |
| 아무 고도, **사진 정확히** 봄 | 아무 값 | 0° | **0°** (중심이라 보정 불필요) |

"위에서 볼수록, 사진 사이로 벗어날수록 더 많이 돌린다"가 숫자로 확인됩니다.

---

## 10. 함정들

- **회전 부호(방향)**: 좌표계 손잡이(handedness)나 UV의 Y가 뒤집혀 있으면 roll이 **반대로**
  보일 수 있습니다. 그러면 `yRot`에 `-1`만 곱하면 됩니다. (베이크로 눈으로 확인하며 맞추는 게 확실)
- **Seam(360°=0°)**: `frac`이 자동으로 이어주므로 열이 `7 → 0`으로 넘어가도 회전이 끊기지 않습니다.
- **극(pole)**: `dir`이 `up`과 완전히 나란하면 `cross(dir,up)=0`이 되어 `hori`가 불안정합니다.
  실제로는 부동소수 오차로 거의 안 걸리고, 걸려도 그 프레임은 대칭이라 회전 방향이 눈에 안 띕니다.
- **`+0.5`를 빼먹으면**: 열 경계와 사진 중심이 어긋나 `floor` 선택과 회전 중심이 안 맞아
  이음매가 벌어집니다. yaw/pitch 둘 다 반드시 반 칸 밀기가 들어갑니다.

---

## 11. 이제 직접 구현 (요약 레시피)

정점 셰이더에서, 위→아래 순서 그대로:

```
1) dir   = normalize(오브젝트공간 카메라위치 - 피벗)
2) hori  = normalize(cross(dir, up));  vertV = cross(hori, dir)
3) verticalAngle = frac(atan2(-dir.z,-dir.x)/2π)*FramesX + 0.5      // 열
   verticalDot   = dir.y                                            // = sin(고도각)
4) yRot = (1/FramesX) * π * verticalDot * (2*frac(verticalAngle)-1) // = sin·δφ
5) cy=cos(yRot); sy=sin(yRot)
   rx =  ex*cy + ey*sy;   ry = -ex*sy + ey*cy                       // ex,ey = 정점 xy
   billboard = hori*rx + vertV*ry                                   // 위치는 회전
6) UV 는 돌리기 전 (ex,ey)로 계산                                    // 텍스처는 제자리
```

이 6줄이 회전 보정의 전부입니다.

- 더 쉬운 개념만: 이 문서 3·6절.
- 베이크 각도·채널·전체 파이프라인까지: [`IMPLEMENTATION.md`](IMPLEMENTATION.md)
- 실제 코드: [`SphericalImpostor.shader`](SphericalImpostor.shader)

> 원본: Amplify Impostors (Amplify Creations)의 Spherical 임포스터 수식 참고.
